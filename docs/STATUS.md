# 상태와 남은 일

2026-09-10 기준.

## 지금 올라가 있는 것

- **메인** — 로고 연출(새로 고침마다) → 글 + 유리 조형물 다섯 겹(등장·떠다니기·
  마우스 시차·스크롤 흩어짐) → 앱 카드 다섯 → 만드는 방식 → 꼬리말
- **상세 다섯 장** — 화면 둘러보기(스토어 샷 띠) → 특징 6 → 앱별 구역 → 여는 법 →
  알아둘 것 → 다른 앱. 앱별 구역: 코인·주식은 실시간 랭킹, 사주는 「폰 밖으로
  나가는 것 / 안 나가는 것」, AI 는 여섯 단계 사다리, 스도쿠는 「지금 어디까지」
- **접근성** — 인터페이스 리뷰(접근성·레이아웃·글·타이포·색·UI) 결과 반영 완료.
  `<main>`, 건너뛰기 링크, 포커스 링, 대비 4.5:1, 히트 영역 40px

## 앱 다섯 개의 상태 (사이트가 말하는 것과 맞아야 한다)

| 앱 | 페이지 | 토스 | 비고 |
| --- | --- | --- | --- |
| 해외 코인 선물 챌린지 | `apps/coin.html` | 출시 | 랭킹 실시간 |
| 국내주식 롱숏 챌린지 | `apps/krx.html` | 출시 | 시세는 전일 종가 — 페이지에 명시돼 있음 |
| 사주 여덟 글자 | `apps/saju.html` | 출시 | |
| 내 AI 활용 점수는? | `apps/ai.html` | 출시 | 09-09 추가 |
| 오늘도 무료 스도쿠 | `apps/sudoku.html` | **미출시** | 웹(`lab/sudoku/`)만. 배지 「웹에서 바로 · 스토어 준비 중」 |

스도쿠가 스토어에 나오면 `tools/build_pages.py` 의 `sudoku` 항목(badge, hint,
status, open_href)과 `index.html` 카드 배지를 바꾼다.

## 남은 일 — 급한 순서

1. **캐시 버스팅.** `style.css`·`site.js`·`app.js` 가 파일명 그대로라 노션·카카오톡
   인앱 브라우저가 옛 파일을 물고 있다. `?v=<내용 해시>` 를 붙이는 작은 스크립트
   하나(`tools/` 에)와 그걸 부르는 습관. 상세 다섯 장은 빌드가 붙이면 되고, 메인·
   개인정보 페이지는 같은 스크립트가 치환한다
2. **`privacy.html` 이 「네 앱 중 둘」이라고 한다.** 앱이 다섯이 됐다. AI 앱은 답을
   기기에만 저장하고 서버로 보내지 않는다(광고 SDK 요청만 있음) — 표에 한 줄 추가
3. **코인 랭킹 5위에 「연결테스트」 행.** 서버(Supabase)에 남은 테스트 데이터라
   사이트에서 고칠 게 아니다. 대표가 SQL 을 실행하면 사라진다. 그때까지 페이지에
   그대로 보인다 — 클라이언트에서 숨기는 꼼수는 넣지 않는다
4. **첫 화면 조형물 조정 여지.** `index.html` 의 `--x/--y/--w` 와 `data-depth`,
   `site.js` 의 떠다니기 폭(`9 + 7 * d`)·시차 폭(`16 * d`, `12 * d`). 실기기에서
   보고 정한다. 아직 실기기 확인 전
5. **AI 앱 OG 이미지.** 다섯 페이지 모두 `img/og.png` 하나를 쓴다. 앱별 OG 를 만들면
   쓰레드·카카오톡 미리보기가 앱마다 달라진다. 1200×630, 검정 바탕, 앱 색 하나
6. **`lab/` 의 파일 크기.** 5.6MB. 이 사이트의 것이 아니라 손대지 않지만, 저장소가
   무거워지면 별도 저장소로 빼는 논의를 할 것

## 하지 않기로 한 것

- 실시간 시세를 홈페이지에 그리는 것 — 국내주식 실시간 시세는 라이선스가 막혀 있다
  (개인사업자는 코스콤 계약 불가). 페이지는 「전일 종가」라고 정직하게 쓴다
- Liquid Glass 류 라이브러리 — html2canvas 기반이라 웹뷰에서 무겁다. 유리 효과는
  `backdrop-filter` 로 충분
- 앱 화면을 AI 로 그리는 것 — 진짜 스크린샷이 있다. 첫 화면 조형물만 AI 그림이다
- 프레임워크·빌드 도구 도입

## 이력

- 09-05 사이트 개설(메인 + 상세 4)
- 09-09 GSAP 도입(제목 낱말 등장·스크롤 시차) / 상세 5장 스토어 형식으로 재구성,
  Supabase 실시간 랭킹 / AI 앱 페이지 추가 / 로고 연출 매번 재생
- 09-10 인터페이스 리뷰 반영(포커스·대비·히트 영역·랜드마크) / 첫 화면 유리 조형물
